name: Publish
on:
  push:
    branches:
      - main
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup pnpm with runtime
        uses: pnpm/setup@v1

      - name: Audit production dependencies
        run: pnpm audit --prod

      - name: Type check - cf-eventhub
        working-directory: cf-eventhub
        run: pnpm typecheck
      - name: Test - cf-eventhub
        working-directory: cf-eventhub
        run: pnpm test

      - name: Check generated Web Console styles
        working-directory: web-console
        run: pnpm check:styles
      - name: Type check - web-console
        working-directory: web-console
        run: pnpm typecheck
      - name: Test - web-console
        working-directory: web-console
        run: pnpm test

      - uses: changesets/action@v1
        with:
          publish: pnpm ci:publish
          commit: Release npm packages
          title: Release npm packages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
